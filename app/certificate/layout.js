export const metadata = {
  title: "Your certificate — CareerLadder",
  description: "Print or save a certificate of completion for a CareerLadder course you've finished.",
  alternates: { canonical: "/certificate" },
  robots: { index: false, follow: true },
};

export default function CertificateLayout({ children }) {
  return children;
}
